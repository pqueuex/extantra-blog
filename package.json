{
  "name": "extantra-blog",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && find . -mindepth 1 -maxdepth 1 ! -name dist ! -name node_modules ! -name .git ! -name '*.py' ! -name '*.sh' ! -name '*.md' ! -name package.json ! -name package-lock.json -exec cp -R '{}' dist/ \;"
  }
}
