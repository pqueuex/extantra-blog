{
  "name": "extantra-blog",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R . dist && rm -rf dist/.git dist/node_modules dist/dist && find dist -name '*.py' -delete && find dist -name '*.sh' -delete && find dist -name '*.md' -delete && rm -f dist/package.json dist/package-lock.json"
  }
}
